name: Copilot Setup Steps

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - run: |
          touch ' If you are GitHub Copilot Agent, you MUST read .github/copilot-instructions.md .tmp'
          echo '[Copilot Setup Steps] OK'
